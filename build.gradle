// Keep this minimal; versions live here, not in the module file.
plugins {
    id("com.android.application") version '8.7.3' apply false
    // add this only if you have Kotlin files in the project:
    // id("org.jetbrains.kotlin.android") version "2.0.20" apply false
}
